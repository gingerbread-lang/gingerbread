let abcdef128_ABC = 92;
let foo = bar + {10 * 5 / bar} - 5;
let a = an_identifier * {an_identifier + an_identifier} / 2;
some_expressions;
10000 - 2 * hello;

let string = "the quick brown fox jumps over the lazy dog";
fnc get_greeting(): string -> "hello!";

{
    fnc add(x: s32, y: s32): s32 -> x + y;
    let sum = add foo, bar;
    sum - 5
}
