[package]
edition = "2018"
license = "MIT OR Apache-2.0"
name = "hir_lower"
version = "0.0.0"

[dependencies]
arena = {path = "../arena"}
ast = {path = "../ast"}
hir = {path = "../hir"}
text-size = "1.1"

[dev-dependencies]
lexer = {path = "../lexer"}
parser = {path = "../parser"}
