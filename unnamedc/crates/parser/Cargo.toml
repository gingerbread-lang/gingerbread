[package]
edition = "2018"
license = "MIT OR Apache-2.0"
name = "parser"
version = "0.1.0"

[dependencies]
drop_bomb = "0.1.5"
rowan = "0.13.2"
syntax = {path = "../syntax"}
text-size = "1.1"
token = {path = "../token"}

[dev-dependencies]
expect-test = "1.1"
lexer = {path = "../lexer"}
