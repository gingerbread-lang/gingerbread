[package]
edition = "2018"
license = "MIT OR Apache-2.0"
name = "ast"
version = "0.0.0"

[dependencies]
syntax = {path = "../syntax"}

[dev-dependencies]
lexer = {path = "../lexer"}
parser = {path = "../parser"}
